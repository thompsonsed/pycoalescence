<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.11">
  <compounddef id="md_necsim_fast-cpp-csv-parser2_README" kind="page">
    <compoundname>md_necsim_fast-cpp-csv-parser2_README</compoundname>
    <title>Fast C++ CSV Parser</title>
    <detaileddescription>
<para>This is a small, easy-to-use and fast header-only library for reading comma separated value (CSV) files.</para><para><heading level="2">Features</heading>
</para><para><itemizedlist>
<listitem><para>Automatically rearranges columns by parsing the header line.</para></listitem><listitem><para>Disk I/O and CSV-parsing are overlapped using threads for efficiency.</para></listitem><listitem><para>Parsing features such as escaped strings can be enabled and disabled at compile time using templates. You only pay in speed for the features you actually use.</para></listitem><listitem><para>Can read multiple GB files in reasonable time.</para></listitem><listitem><para>Support for custom columns separators (i.e. Tab separated value files are supported), quote escaped strings, automatic space trimming.</para></listitem><listitem><para>Works with <computeroutput>*</computeroutput>nix and Windows newlines and automatically ignores UTF-8 BOMs.</para></listitem><listitem><para>Exception classes with enough context to format useful error messages. what() returns error messages ready to be shown to a user.</para></listitem></itemizedlist>
</para><para><heading level="2">Getting Started</heading>
</para><para>The following small example should contain most of the syntax you need to use the library.</para><para><programlisting><codeline><highlight class="preprocessor">#<sp/>include<sp/>&quot;csv.h&quot;</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"></highlight><highlight class="keywordtype">int</highlight><highlight class="normal"><sp/>main(){</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><ref refid="classio_1_1_c_s_v_reader" kindref="compound">io::CSVReader&lt;3&gt;</ref><sp/>in(</highlight><highlight class="stringliteral">&quot;ram.csv&quot;</highlight><highlight class="normal">);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>in.read_header(io::ignore_extra_column,<sp/></highlight><highlight class="stringliteral">&quot;vendor&quot;</highlight><highlight class="normal">,<sp/></highlight><highlight class="stringliteral">&quot;size&quot;</highlight><highlight class="normal">,<sp/></highlight><highlight class="stringliteral">&quot;speed&quot;</highlight><highlight class="normal">);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>std::string<sp/>vendor;<sp/></highlight><highlight class="keywordtype">int</highlight><highlight class="normal"><sp/>size;<sp/></highlight><highlight class="keywordtype">double</highlight><highlight class="normal"><sp/>speed;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keywordflow">while</highlight><highlight class="normal">(in.read_row(vendor,<sp/>size,<sp/>speed)){</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/></highlight><highlight class="comment">//<sp/>do<sp/>stuff<sp/>with<sp/>the<sp/>data</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>}</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting></para><para><heading level="2">Installation</heading>
</para><para>The library only needs a standard conformant C++11 compiler. It has no further dependencies. The library is completely contained inside a single header file and therefore it is sufficient to copy this file to some place on your include path. The library does not have to be explicitly build.</para><para>Note however, that threads are used and some compiler (for example GCC) require you to link against additional librarie to make it work. With GCC it is important to add -lpthread as the last item when linking, i.e. the order in</para><para><programlisting><codeline><highlight class="normal">g++<sp/>-std=c++0x<sp/>a.o<sp/>b.o<sp/>-o<sp/>prog<sp/>-lpthread</highlight></codeline>
</programlisting></para><para>is important. If you for some reason do not want to use threads you can define CSV_IO_NO_THREAD before including the header.</para><para>Remember that the library makes use of C++11 features and therefore you have to enable support for it (f.e. add -std=c++0x or -std=gnu++0x).</para><para>The library was developed and tested with GCC 4.6.1</para><para>Note that VS2013 is not C++11 compilant and will therefore not work out of the box. See <ulink url="https://code.google.com/p/fast-cpp-csv-parser/issues/detail?id=6">here</ulink> for what needs to be adjusted to make the code work.</para><para><heading level="2">Documentation</heading>
</para><para>The libary provides two classes:</para><para><itemizedlist>
<listitem><para><computeroutput>LineReader</computeroutput>: A class to efficiently read large files line by line.</para></listitem><listitem><para><computeroutput>CSVReader</computeroutput>: A class that efficiently reads large CSV files.</para></listitem></itemizedlist>
</para><para>Note that everything is contained in the <computeroutput>io</computeroutput> namespace.</para><para><heading level="3"><computeroutput>LineReader</computeroutput></heading>
</para><para><programlisting><codeline><highlight class="keyword">class<sp/></highlight><highlight class="normal">LineReader{</highlight></codeline>
<codeline><highlight class="normal"></highlight><highlight class="keyword">public</highlight><highlight class="normal">:</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="comment">//<sp/>Constructors</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>LineReader(some_string_type<sp/>file_name);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>LineReader(some_string_type<sp/>file_name,<sp/>std::FILE*source);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>LineReader(some_string_type<sp/>file_name,<sp/>std::istream&amp;source);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>LineReader(some_string_type<sp/>file_name,<sp/>std::unique_ptr&lt;ByteSourceBase&gt;source);</highlight></codeline>
<codeline><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="comment">//<sp/>Reading</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keywordtype">char</highlight><highlight class="normal">*next_line();</highlight></codeline>
<codeline><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="comment">//<sp/>File<sp/>Location</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keywordtype">void</highlight><highlight class="normal"><sp/>set_file_line(</highlight><highlight class="keywordtype">unsigned</highlight><highlight class="normal">);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keywordtype">unsigned</highlight><highlight class="normal"><sp/>get_file_line(</highlight><highlight class="keywordtype">unsigned</highlight><highlight class="normal">)</highlight><highlight class="keyword">const</highlight><highlight class="normal">;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keywordtype">void</highlight><highlight class="normal"><sp/>set_file_name(some_string_type<sp/>file_name);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keyword">const</highlight><highlight class="normal"><sp/></highlight><highlight class="keywordtype">char</highlight><highlight class="normal">*get_truncated_file_name()</highlight><highlight class="keyword">const</highlight><highlight class="normal">;</highlight></codeline>
<codeline><highlight class="normal">};</highlight></codeline>
</programlisting></para><para>The constructor takes a file name and optionally a data source. If no data source is provided the function tries to open the file with the given name and throws an <computeroutput>error::can_not_open_file exception</computeroutput> on failure. If a data source is provided then the file name is only used to format error messages. In that case you can essentially put any string there. Using a string that describes the data source results in more informative error messages.</para><para><computeroutput>some_string_type</computeroutput> can be a <computeroutput>std::string</computeroutput> or a <computeroutput>char*</computeroutput>. If the data source is a <computeroutput>std::FILE*</computeroutput> then the library will take care of calling <computeroutput>std::fclose</computeroutput>. If it is a <computeroutput>std::istream</computeroutput> then the stream is not closed by the library. For best performance open the streams in binary mode. However using text mode also works. <computeroutput>ByteSourceBase</computeroutput> provides an interface that you can use to implement further data sources.</para><para><programlisting><codeline><highlight class="normal">class<sp/>ByteSourceBase{</highlight></codeline>
<codeline><highlight class="normal">public:</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>virtual<sp/>int<sp/>read(char*buffer,<sp/>int<sp/>size)=0;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>virtual<sp/>~ByteSourceBase(){}</highlight></codeline>
<codeline><highlight class="normal">};</highlight></codeline>
</programlisting></para><para>The read function should fill the provided buffer with at most <computeroutput>size</computeroutput> bytes from the data source. It should return the number of bytes actually written to the buffer. If data source has run out of bytes (because for example an end of file was reached) then the function should return 0. If a fatal error occures then you can throw an exception. Note that the function can be called both from the main and the worker thread. However, it is guarenteed that they do not call the function at the same time.</para><para>Lines are read by calling the <computeroutput>next_line</computeroutput> function. It returns a pointer to a null terminated C-string that contains the line. If the end of file is reached a null pointer is returned. The newline character is not included in the string. You may modify the string as long as you do not write past the null terminator. The string stays valid until the destructor is called or until next_line is called again. Windows and <computeroutput>*</computeroutput>nix newlines are handled transparently. UTF-8 BOMs are automatically ignored and missing newlines at the end of the file are no problem.</para><para><bold>Important:</bold> There is a limit of 2^24-1 characters per line. If this limit is exceeded a <computeroutput>error::line_length_limit_exceeded</computeroutput> exception is thrown.</para><para>Looping over all the lines in a file can be done in the following way. <programlisting><codeline><highlight class="normal">LineReader<sp/>in(...);</highlight></codeline>
<codeline><highlight class="normal"></highlight><highlight class="keywordflow">while</highlight><highlight class="normal">(</highlight><highlight class="keywordtype">char</highlight><highlight class="normal">*line<sp/>=<sp/>in.next_line()){</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/>...</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting></para><para>The remaining functions are mainly used used to format error messages. The file line indicates the current position in the file, i.e., after the first <computeroutput>next_line</computeroutput> call it is 1 and after the second 2. Before the first call it is 0. The file name is truncated as internally C-strings are used to avoid <computeroutput>std::bad_alloc</computeroutput> exceptions during error reporting.</para><para><bold>Note:</bold> It is not possible to exchange the line termination character.</para><para><heading level="3"><computeroutput>CSVReader</computeroutput></heading>
</para><para><computeroutput>CSVReader</computeroutput> uses policies. These are classes with only static members to allow core functionality to be exchanged in an efficient way.</para><para><programlisting><codeline><highlight class="keyword">template</highlight><highlight class="normal">&lt;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keywordtype">unsigned</highlight><highlight class="normal"><sp/>column_count,</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keyword">class<sp/></highlight><highlight class="normal">trim_policy<sp/>=<sp/>trim_chars&lt;&apos;<sp/>&apos;,<sp/>&apos;\t&apos;&gt;,<sp/></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keyword">class<sp/></highlight><highlight class="normal">quote_policy<sp/>=<sp/>no_quote_escape&lt;&apos;,&apos;&gt;,</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keyword">class<sp/></highlight><highlight class="normal">overflow_policy<sp/>=<sp/>throw_on_overflow,</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keyword">class<sp/></highlight><highlight class="normal">comment_policy<sp/>=<sp/>no_comment</highlight></codeline>
<codeline><highlight class="normal">&gt;</highlight></codeline>
<codeline><highlight class="normal"></highlight><highlight class="keyword">class<sp/></highlight><highlight class="normal">CSVReader{</highlight></codeline>
<codeline><highlight class="normal"></highlight><highlight class="keyword">public</highlight><highlight class="normal">:</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="comment">//<sp/>Constructors</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="comment">//<sp/>same<sp/>as<sp/>for<sp/>LineReader</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="comment">//<sp/>Parsing<sp/>Header</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keywordtype">void</highlight><highlight class="normal"><sp/>read_header(ignore_column<sp/>ignore_policy,<sp/>some_string_type<sp/>col_name1,<sp/>some_string_type<sp/>col_name2,<sp/>...);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keywordtype">void</highlight><highlight class="normal"><sp/>set_header(some_string_type<sp/>col_name1,<sp/>some_string_type<sp/>col_name2,<sp/>...);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keywordtype">bool</highlight><highlight class="normal"><sp/>has_column(some_string_type<sp/>col_name)</highlight><highlight class="keyword">const</highlight><highlight class="normal">;</highlight></codeline>
<codeline><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="comment">//<sp/>Read</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keywordtype">bool</highlight><highlight class="normal"><sp/>read_row(ColType1&amp;col1,<sp/>ColType2&amp;col2,<sp/>...);</highlight></codeline>
<codeline><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="comment">//<sp/>File<sp/>Location<sp/></highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keywordtype">void</highlight><highlight class="normal"><sp/>set_file_line(</highlight><highlight class="keywordtype">unsigned</highlight><highlight class="normal">);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keywordtype">unsigned</highlight><highlight class="normal"><sp/>get_file_line()</highlight><highlight class="keyword">const</highlight><highlight class="normal">;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keywordtype">void</highlight><highlight class="normal"><sp/>set_file_name(some_string_type<sp/>file_name);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keyword">const</highlight><highlight class="normal"><sp/></highlight><highlight class="keywordtype">char</highlight><highlight class="normal">*get_truncated_file_name()</highlight><highlight class="keyword">const</highlight><highlight class="normal">;</highlight></codeline>
<codeline><highlight class="normal">};</highlight></codeline>
</programlisting></para><para>The <computeroutput>column_count</computeroutput> template parameter indicates how many columns you want to read from the CSV file. This must not necessarily coincide with the actual number of columns in the file. The three policies govern various aspects of the parsing.</para><para>The trim policy indicates what characters should be ignored at the begin and the end of every column. The default ignores spaces and tabs. This makes sure that</para><para><programlisting><codeline><highlight class="normal">a,b,c</highlight></codeline>
<codeline><highlight class="normal">1,2,3</highlight></codeline>
</programlisting></para><para>is interpreted in the same way as</para><para><programlisting><codeline><highlight class="normal"><sp/><sp/>a,<sp/>b,<sp/><sp/><sp/>c</highlight></codeline>
<codeline><highlight class="normal">1<sp/><sp/>,<sp/>2,<sp/><sp/><sp/>3</highlight></codeline>
</programlisting></para><para>The trim_chars can take any number of template parameters. For example `trim_chars&lt;&apos; &apos;, &apos;&apos;, &apos;_&apos;&gt; <computeroutput>is also valid. If no character should be trimmed use</computeroutput>trim_chars&lt;&gt;`.</para><para>The quote policy indicates how string should be escaped. It also specifies the column separator. The predefined policies are:</para><para><itemizedlist>
<listitem><para><computeroutput>no_quote_escape&lt;sep&gt;</computeroutput> : Strings are not escaped. &quot;`sep`&quot; is used as column separator.</para></listitem><listitem><para><computeroutput>double_quote_escape&lt;sep, quote&gt;</computeroutput> : Strings are escaped using quotes. Quotes are escaped using two consecutive quotes. &quot;`sep`&quot; is used as column separator and &quot;`quote`&quot; as quoting character.</para></listitem></itemizedlist>
</para><para><bold>Important</bold>: When combining trimming and quoting the rows are first trimmed and then unquoted. A consequence is that spaces inside the quotes will be conserved. If you want to get rid of spaces inside the quotes, you need to remove them yourself.</para><para><bold>Important</bold>: Quoting can be quite expensive. Disable it if you do not need it.</para><para>The overflow policy indicates what should be done if the integers in the input are too large to fit into the variables. There following policies are predefined:</para><para><itemizedlist>
<listitem><para><computeroutput>throw_on_overflow</computeroutput> : Throw an <computeroutput>error::integer_overflow</computeroutput> or <computeroutput>error::integer_underflow</computeroutput> exception.</para></listitem><listitem><para><computeroutput>ignore_overflow</computeroutput> : Do nothing and let the overflow happen.</para></listitem><listitem><para><computeroutput>set_to_max_on_overflow</computeroutput> : Set the value to <computeroutput>numeric_limits&lt;...&gt;::max()</computeroutput> (or to the min-pendant).</para></listitem></itemizedlist>
</para><para>The comment policy allows to skip lines based on some criteria. Valid predefined policies are:</para><para><itemizedlist>
<listitem><para><computeroutput>no_comment</computeroutput> : Do not ignore any line.</para></listitem><listitem><para><computeroutput>empty_line_comment</computeroutput> : Ignore all lines that are empty or only contains spaces and tabs.</para></listitem><listitem><para><computeroutput>single_line_comment&lt;com1, com2, ...&gt;</computeroutput> : Ignore all lines that start with com1 or com2 or ... as the first character. There may not be any space between the beginning of the line and the comment character.</para></listitem><listitem><para><computeroutput>single_and_empty_line_comment&lt;com1, com2, ...&gt;</computeroutput> : Ignore all empty lines and single line comments.</para></listitem></itemizedlist>
</para><para>Examples:</para><para><itemizedlist>
<listitem><para>`CSVReader&lt;4, trim_chars&lt;&apos; &apos;&gt;, double_quote_escape&lt;&apos;,&apos;,&apos;"&apos;&gt; &gt;<computeroutput>reads 4 columns from a normal CSV file with string escaping enabled. *</computeroutput>CSVReader&lt;3, trim_chars&lt;&apos; &apos;&gt;, no_quote_escape&lt;&apos;&apos;&gt;, single_line_comment&lt;&apos;#&apos;&gt; &gt;` reads 3 columns from a tab separated file with string escaping disabled. Lines starting with a # are ignored.</para></listitem></itemizedlist>
</para><para>The constructors and the file location functions are exactly the same as for <computeroutput>LineReader</computeroutput>. See its documentation for details.</para><para>There are three methods that deal with headers. The <computeroutput>read_header</computeroutput> methods reads a line from the file and rearranges the columns to match that order. It also checks whether all necessary columns are present. The <computeroutput>set_header</computeroutput> method does <emphasis>not</emphasis> read any input. Use it if the file does not have any header. Obviously it is impossible to rearrange columns or check for their availability when using it. The order in the file and in the program must match when using <computeroutput>set_header</computeroutput>. The <computeroutput>has_column</computeroutput> method checks whether a column is present in the file. The first argument of <computeroutput>read_header</computeroutput> is a bitfield that determines how the function should react to column mismatches. The default behavior is to throw an <computeroutput>error::extra_column_in_header</computeroutput> exception if the file contains more columns than expected and an <computeroutput>error::missing_column_in_header</computeroutput> when there are not enough. This behavior can be altered using the following flags.</para><para><itemizedlist>
<listitem><para><computeroutput>ignore_no_column</computeroutput>: The default behavior, no flags are set</para></listitem><listitem><para><computeroutput>ignore_extra_column</computeroutput>: If a column with a name is in the file but not in the argument list, then it is silently ignored.</para></listitem><listitem><para><computeroutput>ignore_missing_column</computeroutput>: If a column with a name is not in the file but is in the argument list, then <computeroutput>read_row</computeroutput> will not modify the corresponding variable.</para></listitem></itemizedlist>
</para><para>When using <computeroutput>ignore_column_missing</computeroutput> it is a good idea to initialize the variables passed to <computeroutput>read_row</computeroutput> with a default value, for example:</para><para><programlisting><codeline><highlight class="comment">//<sp/>The<sp/>file<sp/>only<sp/>contains<sp/>column<sp/>&quot;a&quot;</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal">CSVReader&lt;2&gt;in(...);</highlight></codeline>
<codeline><highlight class="normal">in.read_header(ignore_missing_column,<sp/></highlight><highlight class="stringliteral">&quot;a&quot;</highlight><highlight class="normal">,<sp/></highlight><highlight class="stringliteral">&quot;b&quot;</highlight><highlight class="normal">);</highlight></codeline>
<codeline><highlight class="normal"></highlight><highlight class="keywordtype">int</highlight><highlight class="normal"><sp/>a,b<sp/>=<sp/>42;</highlight></codeline>
<codeline><highlight class="normal"></highlight><highlight class="keywordflow">while</highlight><highlight class="normal">(in.read_row(a,b)){</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="comment">//<sp/>a<sp/>contains<sp/>the<sp/>value<sp/>from<sp/>the<sp/>file</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="comment">//<sp/>b<sp/>is<sp/>left<sp/>unchanged<sp/>by<sp/>read_row,<sp/>i.e.,<sp/>it<sp/>is<sp/>42</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting></para><para>If only some columns are optional or their default value depends on other columns you have to use <computeroutput>has_column</computeroutput>, for example:</para><para><programlisting><codeline><highlight class="comment">//<sp/>The<sp/>file<sp/>only<sp/>contains<sp/>the<sp/>columns<sp/>&quot;a&quot;<sp/>and<sp/>&quot;b&quot;</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal">CSVReader&lt;2&gt;in(...);</highlight></codeline>
<codeline><highlight class="normal">in.read_header(ignore_missing_column,<sp/></highlight><highlight class="stringliteral">&quot;a&quot;</highlight><highlight class="normal">,<sp/></highlight><highlight class="stringliteral">&quot;b&quot;</highlight><highlight class="normal">,<sp/></highlight><highlight class="stringliteral">&quot;sum&quot;</highlight><highlight class="normal">);</highlight></codeline>
<codeline><highlight class="normal"></highlight><highlight class="keywordflow">if</highlight><highlight class="normal">(!in.has_column(</highlight><highlight class="stringliteral">&quot;a&quot;</highlight><highlight class="normal">)<sp/>||<sp/>!in.has_column(</highlight><highlight class="stringliteral">&quot;b&quot;</highlight><highlight class="normal">))</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keywordflow">throw</highlight><highlight class="normal"><sp/>my_neat_error_class();</highlight></codeline>
<codeline><highlight class="normal"></highlight><highlight class="keywordtype">bool</highlight><highlight class="normal"><sp/>has_sum<sp/>=<sp/>in.has_column(</highlight><highlight class="stringliteral">&quot;sum&quot;</highlight><highlight class="normal">);</highlight></codeline>
<codeline><highlight class="normal"></highlight><highlight class="keywordtype">int</highlight><highlight class="normal"><sp/>a,b,sum;</highlight></codeline>
<codeline><highlight class="normal"></highlight><highlight class="keywordflow">while</highlight><highlight class="normal">(in.read_row(a,b,sum)){</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/></highlight><highlight class="keywordflow">if</highlight><highlight class="normal">(!has_sum)</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>sum<sp/>=<sp/>a+b;</highlight></codeline>
<codeline><highlight class="normal">}</highlight></codeline>
</programlisting></para><para><bold>Important</bold>: Do not call <computeroutput>has_column</computeroutput> from within the read-loop. It would work correctly but significantly slowdown processing.</para><para>If two columns have the same name an error::duplicated_column_in_header exception is thrown. If <computeroutput>read_header</computeroutput> is called but the file is empty a <computeroutput>error::header_missing</computeroutput> exception is thrown.</para><para>The <computeroutput>read_row</computeroutput> function reads a line, splits it into the columns and arranges them correctly. It trims the entries and unescapes them. If requested the content is interpreted as integer or as floating point. The variables passed to read_row may be of the following types.</para><para><itemizedlist>
<listitem><para>builtin signed integer: These are <computeroutput>signed char</computeroutput>, <computeroutput>short</computeroutput>, <computeroutput>int</computeroutput>, <computeroutput>long</computeroutput> and <computeroutput>long long</computeroutput>. The input must be encoded as a base 10 ASCII number optionally preceded by a + or -. The function detects whether the integer is too large would overflow (or underflow) and behaves as indicated by overflow_policy.</para></listitem><listitem><para>builtin unsigned integer: Just as the signed counterparts except that a leading + or - is not allowed.</para></listitem><listitem><para>builtin floating point: These are <computeroutput>float</computeroutput>, <computeroutput>double</computeroutput> and <computeroutput>long double</computeroutput>. The input may have a leading + or -. The number must be base 10 encoded. The decimal point may either be a dot or a comma. (Note that a comma will only work if it is not also used as column separator or the number is escaped.) A base 10 exponent may be specified using the &quot;1e10&quot; syntax. The &quot;e&quot; may be lower- or uppercase. Examples for valid floating points are &quot;1&quot;, &quot;-42.42&quot; and &quot;+123.456E789&quot;. The input is rounded to the next floating point or infinity if it is too large or small.</para></listitem><listitem><para><computeroutput>char</computeroutput>: The column content must be a single character.</para></listitem><listitem><para><computeroutput>std::string</computeroutput>: The column content is assigned to the string. The std::string is filled with the trimmed and unescaped version.</para></listitem><listitem><para><computeroutput>char*</computeroutput>: A pointer directly into the buffer. The string is trimmed and unescaped and null terminated. This pointer stays valid until read_row is called again or the CSVReader is destroyed. Use this for user defined types.</para></listitem></itemizedlist>
</para><para>Note that there is no inherent overhead to using <computeroutput>char*</computeroutput> and then interpreting it compared to using one of the parsers directly build into <computeroutput>CSVReader</computeroutput>. The builtin number parsers are pure convenience. If you need a slightly different syntax then use <computeroutput>char*</computeroutput> and do the parsing yourself.</para><para><heading level="2">FAQ</heading>
</para><para>Q: The library is throwing a std::system_error with code -1. How to get it to work?</para><para>A: Your compiler&apos;s std::thread implementation is broken. Define CSV_IO_NO_THREAD to disable threading support.</para><para>Q: My values are not just ints or strings. I want to parse my customized type. Is this possible?</para><para>A: Read a <computeroutput>char*</computeroutput> and parse the string. At first this seems expensive but it is not as the pointer you get points directly into the memory buffer. In fact there is no inherent reason why a custom int-parser realized this way must be any slower than the int-parser build into the library. By reading a <computeroutput>char*</computeroutput> the library takes care of column reordering and quote escaping and leaves the actual parsing to you. Note that using a std::string is slower as it involves a memory copy.</para><para>Q: I get lots of compiler errors when compiling the header! Please fix it. :(</para><para>A: Have you enabled the C++11 mode of your compiler? If you use GCC you have to add -std=c++0x to the commandline. If this does not resolve the problem, then please open a ticket.</para><para>Q: The library crashes when parsing large files! Please fix it. :(</para><para>A: When using GCC have you linked against -lpthread? Read the installation section for details on how to do this. If this does not resolve the issue then please open a ticket. (The reason why it only crashes only on large files is that the first chuck is read synchronous and if the whole file fits into this chuck then no asynchronous call is performed.) Alternatively you can define CSV_IO_NO_THREAD.</para><para>Q: Does the library support UTF?</para><para>A: The library has basic UTF-8 support, or to be more precise it does not break when passing UTF-8 strings through it. If you read a <computeroutput>char*</computeroutput> then you get a pointer to the UTF-8 string. You will have to decode the string on your own. The separator, quoting, and commenting characters used by the library can only be ASCII characters. </para>    </detaileddescription>
  </compounddef>
</doxygen>
